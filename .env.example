# PostgreSQL connection
DB_HOST=31.97.218.31
DB_PORT=5432
DB_USER=kryosenseadmin
DB_PASSWORD=kryosense2025
# Main branch points to production schema
DB_NAME=kryosense

# App
PORT=3000
JWT_SECRET=please_change_me
# Optional for localhost when there is no subdomain
# Example: DEFAULT_TENANT=tenant_brandon
DEFAULT_TENANT=

# Runtime
NODE_ENV=development
