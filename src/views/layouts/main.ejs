<!DOCTYPE html>
<html lang="es" data-theme="<%= theme === 'light' ? 'kryoLight' : 'kryoDark' %>">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="theme-color" content="#6d5efc" />
  <meta name="mobile-web-app-capable" content="yes" />
  <meta name="application-name" content="KryoSense" />
  <title><%= (typeof title !== 'undefined' && title) ? title : 'Kryotecsense' %></title>
  <link rel="icon" type="image/png" href="/static/images/favicon.png" />
  <link rel="manifest" href="/manifest.webmanifest?v=<%= assetVersion %>" />
  <meta name="apple-mobile-web-app-capable" content="yes" />
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
  <meta name="apple-mobile-web-app-title" content="KryoSense" />
  <link rel="apple-touch-icon" sizes="180x180" href="/icons/app-180.png?v=5" />
  <link rel="stylesheet" href="/static/css/app.css?v=<%= assetVersion %>" />
  <link rel="stylesheet" href="/static/css/overrides-acond.css?v=<%= assetVersion %>" />
</head>
<body class="min-h-screen">
  <div class="drawer lg:drawer-open">
    <input id="drawer-nav" type="checkbox" class="drawer-toggle" />
    <label for="drawer-nav" aria-label="Abrir menú" class="mobile-menu-btn lg:hidden fixed top-3 left-3 z-50 btn btn-square btn-sm btn-primary shadow-md">
      <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" /></svg>
    </label>
    <div class="drawer-content flex flex-col">
      <section class="p-4 pt-16 lg:pt-4 animate-fade-in">
        <% if (typeof securityWarning !== 'undefined' && securityWarning) { %>
          <div class="alert alert-warning shadow mb-4">
            <span><%= securityWarning %></span>
          </div>
        <% } %>
        <%- body %>
      </section>
    </div>
    <div class="drawer-side z-50">
      <label for="drawer-nav" aria-label="close sidebar" class="drawer-overlay"></label>
      <aside class="min-h-full w-72 p-4 border-r border-base-300/40 flex flex-col bg-base-200/95 backdrop-blur-md relative">
        <div class="mb-4 px-2 text-lg font-bold flex items-center justify-between k-title-gradient">
          <img src="/static/images/KryoSenseLogo%20Con%20Slogan.jpg" alt="KryoSense 360° Thermal Packaging Control" class="h-14 max-w-[180px] object-contain" />
          <label for="drawer-nav" aria-label="close sidebar" class="lg:hidden btn btn-ghost btn-xs">✕</label>
        </div>
        <button id="btn-theme-toggle" class="btn k-gradient-btn btn-sm mb-4 text-xs justify-start">Cambiar tema</button>
        <ul class="menu flex-1">
          <% const rawRoles = Array.isArray(user?.roles) && user.roles.length ? user.roles : ((user && user.rol) ? [user.rol] : []); %>
          <% const mapLegacy = {
            preacond:'acondicionador',
            operacion:'operador',
            bodega:'bodeguero',
            inspeccion:'inspeccionador',
            admin:'admin',
            administrador:'admin',
            super_admin:'super_admin',
            superadmin:'super_admin',
            'super admin':'super_admin',
            'super-admin':'super_admin',
            'super administrador':'super_admin'
          }; %>
          <% const canonicalRoles = []; const seenRoles = new Set(); %>
          <% rawRoles.forEach((r) => {
               const key = (r || '').toString().toLowerCase();
               const normalized = mapLegacy[key] || key;
               if (normalized && !seenRoles.has(normalized)) { seenRoles.add(normalized); canonicalRoles.push(normalized); }
             }); %>
          <% if (!canonicalRoles.length) {
               const fallbackKey = ((user && (user.rol || '')) || '').toString().toLowerCase();
               const fallback = mapLegacy[fallbackKey] || fallbackKey;
               if (fallback) { canonicalRoles.push(fallback); }
             } %>
          <% const rolesSet = new Set(canonicalRoles.filter(Boolean)); %>
          <% const primaryRole = canonicalRoles[0] || ''; %>
          <% const hasSede = user && !(user.sede_id === null || user.sede_id === undefined || user.sede_id === ''); %>
          <% const isSuperAdmin = !hasSede || rolesSet.has('super_admin'); %>
          <% const limitedRoles = ['acondicionador','operador','bodeguero','inspeccionador']; %>
          <% const hasExtendedRole = Array.from(rolesSet).some((r) => !limitedRoles.includes(r)); %>
          <% const hasRole = (value) => rolesSet.has(value); %>
          <% const hasAdminRole = hasRole('admin') || hasRole('super_admin'); %>
          <% if (isSuperAdmin) { %>
            <li><a class="<%= currentPath.startsWith('/dashboard') ? 'active' : '' %>" href="/dashboard">Dashboard</a></li>
            <li><a class="<%= currentPath.startsWith('/inventario') ? 'active' : '' %>" href="/inventario">Inventario</a></li>
            <li><a class="<%= currentPath.startsWith('/administracion') ? 'active' : '' %>" href="/administracion">Administración</a></li>
            <li><a class="<%= currentPath.startsWith('/reportes') ? 'active' : '' %>" href="/reportes">Reportes</a></li>
          <% } else if (hasExtendedRole) { %>
            <li><a class="<%= currentPath.startsWith('/dashboard') ? 'active' : '' %>" href="/dashboard">Dashboard</a></li>
            <li><a class="<%= currentPath.startsWith('/registro') ? 'active' : '' %>" href="/registro">Registro</a></li>
            <li><a class="<%= currentPath.startsWith('/inventario') ? 'active' : '' %>" href="/inventario">Inventario</a></li>
            <li><a class="<%= currentPath.startsWith('/ordenes') && !currentPath.startsWith('/ordenes/calculadora') ? 'active' : '' %>" href="/ordenes">Órdenes</a></li>
            <li><a class="<%= currentPath.startsWith('/ordenes/calculadora') ? 'active' : '' %>" href="/ordenes/calculadora">Calculadora de órdenes</a></li>
          <% } %>
          <% if (!isSuperAdmin && hasRole('bodeguero')) { %>
            <li><a class="<%= currentPath.startsWith('/registro') ? 'active' : '' %>" href="/registro">Registro</a></li>
          <% } %>
          <% if (!isSuperAdmin) { %>
          <li>
            <details <%= currentPath.startsWith('/operacion') ? 'open' : '' %>>
              <summary>Operación</summary>
              <ul>
                <% if (hasExtendedRole) { %>
                  <li><a class="<%= currentPath === '/operacion/todas' ? 'active' : '' %>" href="/operacion/todas">Todas las fases</a></li>
                  <li><a class="<%= currentPath === '/operacion/bodega' ? 'active' : '' %>" href="/operacion/bodega">En bodega</a></li>
                <% } %>
                <% if (hasRole('bodeguero')) { %>
                  <li><a class="<%= currentPath === '/operacion/bodega' ? 'active' : '' %>" href="/operacion/bodega">En bodega</a></li>
                <% } %>
                <% if (hasRole('acondicionador') || hasExtendedRole) { %>
                  <li><a class="<%= currentPath === '/operacion/preacond' ? 'active' : '' %>" href="/operacion/preacond">Preacondicionamiento</a></li>
                  <li><a class="<%= currentPath === '/operacion/acond' ? 'active' : '' %>" href="/operacion/acond">Acondicionamiento</a></li>
                <% } %>
                <% if (hasRole('operador') || hasExtendedRole) { %>
                  <li><a class="<%= currentPath === '/operacion/operacion' ? 'active' : '' %>" href="/operacion/operacion">Operación</a></li>
                  <% if (hasRole('operador')) { %>
                    <li><a class="<%= currentPath === '/operacion/devolucion' ? 'active' : '' %>" href="/operacion/devolucion">Devolución</a></li>
                  <% } %>
                <% } %>
                <% if (hasRole('inspeccionador')) { %>
                  <li><a class="<%= currentPath === '/operacion/inspeccion' ? 'active' : '' %>" href="/operacion/inspeccion">Inspección</a></li>
                <% } %>
                <% if (hasExtendedRole) { %>
                  <li><a class="<%= currentPath === '/operacion/devolucion' ? 'active' : '' %>" href="/operacion/devolucion">Devolución</a></li>
                  <li><a class="<%= currentPath === '/operacion/inspeccion' ? 'active' : '' %>" href="/operacion/inspeccion">Inspección</a></li>
                <% } %>
                <% if (hasExtendedRole || hasAdminRole) { %>
                  <!-- Cronómetros predeterminados se movió a Administración -->
                <% } %>
              </ul>
            </details>
          </li>
          <% } %>
          <% if (!isSuperAdmin && (hasRole('operador') || hasAdminRole)) { %>
            <li><a class="<%= currentPath.startsWith('/traslado') ? 'active' : '' %>" href="/traslado">Traslado</a></li>
          <% } %>
          <% if (hasAdminRole && hasSede) { %>
            <li><a class="<%= currentPath.startsWith('/auditoria') ? 'active' : '' %>" href="/auditoria">Auditoría</a></li>
          <% } %>
          <% if (hasAdminRole && hasSede) { %>
            <li><a class="<%= currentPath.startsWith('/administracion') ? 'active' : '' %>" href="/administracion">Administración</a></li>
            <li><a class="<%= currentPath.startsWith('/administracion/config-tiempos') ? 'active' : '' %>" href="/administracion/config-tiempos">Cronómetros predeterminados</a></li>
            <li><a class="<%= currentPath.startsWith('/reportes') ? 'active' : '' %>" href="/reportes">Reportes</a></li>
          <% } %>
        </ul>
        <div class="mt-2">
          <form id="logout-form" method="post" action="/auth/logout"></form>
          <div class="dropdown dropdown-top w-full">
            <label tabindex="0" class="btn btn-ghost w-full justify-between">
              <div class="flex items-center gap-3">
                <div class="avatar placeholder">
                  <div class="bg-primary text-primary-content rounded-full w-8">
                    <span class="text-sm">
                      <%= ((user && user.nombre) ? String(user.nombre).charAt(0) : 'U').toUpperCase() %>
                    </span>
                  </div>
                </div>
                <div class="text-left leading-tight">
                  <div class="text-sm font-semibold truncate max-w-[140px]">
                    <%= (user && (user.nombre || user.correo)) ? (user.nombre || user.correo) : 'Usuario' %>
                  </div>
                  <div class="text-[11px] opacity-60 truncate max-w-[160px]">
                    <%= user && user.sede_nombre
                      ? user.sede_nombre
                      : (hasSede && user && user.sede_id)
                        ? `Sede ${user.sede_id}`
                        : 'Todas las sedes' %>
                  </div>
                </div>
              </div>
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 opacity-70" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 11.111l3.71-3.88a.75.75 0 111.08 1.04l-4.24 4.43a.75.75 0 01-1.08 0L5.21 8.27a.75.75 0 01.02-1.06z" clip-rule="evenodd"/></svg>
            </label>
            <ul tabindex="0" class="dropdown-content menu p-2 shadow bg-base-100 rounded-box w-60 mb-2">
              <li><a href="/cuenta" id="user-settings">Configuración</a></li>
              <li><a href="/notificaciones" id="user-notifs">Notificaciones</a></li>
              <li><button type="submit" form="logout-form" class="text-error">Cerrar sesión</button></li>
            </ul>
          </div>
        </div>
      </aside>
    </div>
  </div>
  <script>window.__IDLE_MINUTES__ = <%= typeof idleMinutes !== 'undefined' ? idleMinutes : (user?.sesion_ttl_minutos || 10) %>;</script>
  <script src="/static/js/theme.js?v=<%= assetVersion %>"></script>
  <script src="/static/js/ux.js?v=<%= assetVersion %>"></script>
  <script src="/static/js/idle-logout.js?v=<%= assetVersion %>" defer></script>
  <script src="/static/js/user-menu.js?v=<%= assetVersion %>" defer></script>
  <script src="/static/js/notifications.js?v=<%= assetVersion %>" defer></script>
  <script src="/static/js/pwa.js?v=<%= assetVersion %>" defer></script>
  <style>
    #drawer-nav:checked + label.mobile-menu-btn { display:none }
    @media (max-width: 1023px){
      #drawer-nav:checked ~ .drawer-content { overflow:hidden; }
    }
  </style>
</body>
</html>
