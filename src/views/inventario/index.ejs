<% if (typeof error !== 'undefined' && error === 'eliminar') { %>
  <div class="alert alert-error mb-4">
    <span>Ocurrió un error al eliminar el registro.</span>
  </div>
<% } %>
<% title = 'Inventario' %>

<div class="mb-2">
  <p class="text-sm opacity-70">Gestión de credocubes y componentes RFID</p>
  <form method="get" action="/inventario" class="mt-2" id="inv-search-form">
    <div class="form-control">
      <label class="input input-bordered flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="opacity-70"><circle cx="11" cy="11" r="8"></circle><path d="m21 21-4.3-4.3"></path></svg>
  <input type="search" class="grow" placeholder="Buscar / Escanear RFIDs (flujo continuo)" name="q" value="<%= q || '' %>" maxlength="24" id="inv-search-input" autocomplete="off" />
      </label>
    </div>
    <div class="text-xs opacity-60 mt-1" id="inv-search-count">0/24 caracteres</div>
    <% if (multiScan) { %>
      <input type="hidden" name="rfids" value="<%= scannedRfids.join(',') %>" id="inv-rfids-hidden" />
    <% } %>
    <div class="mt-2 space-y-2">
      <div class="flex flex-wrap gap-2" id="inv-rfid-tags">
        <% if (multiScan) { scannedRfids.forEach(r => { %>
          <span class="badge badge-primary gap-1" data-rfid-tag="<%= r %>">
            <%= r %>
            <button type="button" class="ml-1 text-error" data-action="remove-rfid" data-rfid="<%= r %>">✕</button>
          </span>
        <% }) } %>
      </div>
      <div class="text-xs opacity-70">
        <% if (multiScan) { %>
          Escaneando múltiples RFIDs ( <%= scannedRfids.length %> ). Cada nuevo bloque de 24 caracteres se añade automáticamente.
          <a href="/inventario" class="link link-error ml-2" id="inv-clear-multiscan">Limpiar</a>
        <% } else { %>
          Escanea varios RFIDs de forma continua; cada 24 caracteres se detecta un RFID y se añade automáticamente. Con un único RFID (sin multi-scan) se muestra su caja completa.
        <% } %>
      </div>
    </div>
    <% if (!multiScan && !cajaMode) { %>
      <div class="mt-3 flex flex-wrap gap-2">
        <a class="btn btn-sm <%= !cat ? 'btn-primary' : '' %>" href="/inventario<%= q ? ('?q='+encodeURIComponent(q)) : '' %>">Todos (<%= counts?.total || 0 %>)</a>
        <a class="btn btn-sm <%= cat==='cubes' ? 'btn-primary' : '' %>" href="/inventario?cat=cubes<%= q ? ('&q='+encodeURIComponent(q)) : '' %>">Cubes (<%= counts?.cubes || 0 %>)</a>
        <a class="btn btn-sm <%= cat==='vips' ? 'btn-primary' : '' %>" href="/inventario?cat=vips<%= q ? ('&q='+encodeURIComponent(q)) : '' %>">VIPs (<%= counts?.vips || 0 %>)</a>
        <a class="btn btn-sm <%= cat==='tics' ? 'btn-primary' : '' %>" href="/inventario?cat=tics<%= q ? ('&q='+encodeURIComponent(q)) : '' %>">TICs (<%= counts?.tics || 0 %>)</a>
      </div>
    <% } else { %>
      <div class="mt-3 text-xs opacity-70">Categorías ocultas mientras está activo multi-scan o modo caja.</div>
    <% } %>
    <% if (!multiScan && !cajaMode) { %>
      <div class="mt-3 flex flex-wrap items-center gap-2">
        <label class="label text-xs opacity-70">Estado:</label>
        <div class="join">
          <a class="btn btn-xs join-item <%= !state ? 'btn-primary' : '' %>" href="/inventario<%= q?('?q='+encodeURIComponent(q)) : '' %><%= cat?((q?'&':'?')+'cat='+encodeURIComponent(cat)) : '' %>">Todos</a>
          <a class="btn btn-xs join-item <%= state==='inhabilitado' ? 'btn-primary' : '' %>" href="/inventario?<%= q?('q='+encodeURIComponent(q)+'&') : '' %><%= cat?('cat='+encodeURIComponent(cat)+'&') : '' %>state=inhabilitado">Inhabilitado</a>
        </div>
      </div>
    <% } %>
  </form>
</div>

<div class="card bg-base-200 shadow-lg transition-all hover:shadow-xl">
  <div class="card-body animate-slide-up">
    <div class="overflow-x-auto">
      <table class="table table-zebra">
        <thead>
          <tr>
            <th>NOMBRE UNIDAD</th>
            <th>MODELO ID</th>
            <th>RFID</th>
            <th>LOTE</th>
            <th>ESTADO</th>
            <th>SUB-ESTADO</th>
            <th>CATEGORÍA</th>
            <th>ÚLTIMA ACTUALIZACIÓN</th>
            <th>ACCIONES</th>
          </tr>
        </thead>
        <tbody id="inv-tbody">
          <% if(items && items.length){ items.forEach(i => { %>
            <tr>
              <td><%= i.nombre_unidad %></td>
              <td><%= i.modelo_id %></td>
              <td><%= i.rfid %></td>
              <td class="whitespace-nowrap"><%= i.lote || '' %></td>
              <td class="whitespace-nowrap"><div class="badge badge-primary badge-outline hover:badge-secondary transition-colors whitespace-nowrap"><%= i.estado %></div></td>
              <td class="whitespace-nowrap"><% if (i.sub_estado) { %><div class="badge badge-outline whitespace-nowrap"><%= i.sub_estado %></div><% } else { %><span class="opacity-60">—</span><% } %></td>
              <td><%= i.categoria %></td>
              <td><%= new Date(i.ultima_actualizacion).toLocaleString() %></td>
              <td class="whitespace-nowrap">
                <div class="join">
                  <button type="button" class="btn btn-ghost btn-xs join-item" data-action="inv-edit"
                    data-id="<%= i.id %>" data-modelo_id="<%= i.modelo_id %>" data-rfid="<%- (i.rfid||'').replace(/\"/g,'&quot;') %>"
                    data-nombre="<%- (i.nombre_unidad||'').replace(/\"/g,'&quot;') %>" data-lote="<%- (i.lote||'').replace(/\"/g,'&quot;') %>"
                    data-estado="<%- (i.estado||'').replace(/\"/g,'&quot;') %>" data-sub="<%- (i.sub_estado||'').replace(/\"/g,'&quot;') %>">
                    ✎
                  </button>
                  <form method="post" action="/inventario/<%= i.id %>/delete" class="join-item inv-delete-form">
                    <button class="btn btn-ghost btn-xs text-error" aria-label="Eliminar">🗑</button>
                  </form>
                </div>
              </td>
            </tr>
          <% }) } else { %>
            <tr><td colspan="9" class="text-center py-4 opacity-60">Sin resultados</td></tr>
          <% } %>
        </tbody>
      </table>
    </div>
    <div class="mt-3 flex items-center justify-between text-xs">
      <div id="inv-status">Mostrando <%= items.length %> de <%= total %> resultados</div>
      <div class="text-right"><span class="badge badge-outline" id="inv-mode-badge">Inicial</span></div>
    </div>
  </div>
</div>

<!-- Modal: Editar item inventario -->
<dialog id="inv-edit-modal" class="modal">
  <div class="modal-box max-w-lg">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
    </form>
    <h3 class="font-bold text-lg mb-2">Editar item</h3>
    <form id="inv-edit-form" method="post" action="/inventario/0/update" class="space-y-3">
      <div class="grid grid-cols-2 gap-3">
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Modelo ID</span>
          <input id="inv-edit-modelo_id" name="modelo_id" type="text" class="input input-sm input-bordered" readonly />
        </label>
        <label class="form-control">
          <span class="label-text text-xs opacity-70">RFID</span>
          <input id="inv-edit-rfid" name="rfid" type="text" class="input input-sm input-bordered" readonly />
        </label>
      </div>
      <div class="grid grid-cols-2 gap-3">
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Nombre unidad</span>
          <input id="inv-edit-nombre" name="nombre_unidad" type="text" class="input input-sm input-bordered" autocomplete="off" />
        </label>
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Lote</span>
          <input id="inv-edit-lote" name="lote" type="text" class="input input-sm input-bordered" autocomplete="off" />
        </label>
      </div>
      <div class="grid grid-cols-2 gap-3">
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Estado</span>
          <input id="inv-edit-estado" name="estado" type="text" class="input input-sm input-bordered" autocomplete="off" />
        </label>
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Sub-estado</span>
          <input id="inv-edit-sub" name="sub_estado" type="text" class="input input-sm input-bordered" autocomplete="off" />
        </label>
      </div>
      <div class="flex items-center justify-between mt-2">
        <span id="inv-edit-msg" class="text-xs opacity-70"></span>
        <div class="join">
          <button type="button" class="btn btn-ghost btn-sm join-item" id="inv-edit-cancel">Cancelar</button>
          <button type="submit" class="btn btn-primary btn-sm join-item">Guardar</button>
        </div>
      </div>
    </form>
  </div>
  <form method="dialog" class="modal-backdrop"><button>close</button></form>
</dialog>

<script src="/static/js/inventario.js?v=<%= assetVersion %>"></script>
