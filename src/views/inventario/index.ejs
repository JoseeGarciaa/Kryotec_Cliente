<% if (typeof error !== 'undefined' && (error === 'inhabilitar' || error === 'eliminar')) { %>
  <div class="alert alert-error mb-4">
    <span>Ocurrió un error al inhabilitar el registro.</span>
  </div>
<% } %>
<% if (typeof error !== 'undefined' && error === 'rfid_activo_otro_tenant') { %>
  <div class="alert alert-error mb-4">
    <span>La pieza ya se encuentra habilitada en otro inventario. Desactívala allí antes de habilitarla aquí.</span>
  </div>
<% } %>
<% title = 'Inventario' %>
<%
  const safePage = Number(page) > 0 ? Number(page) : 1;
  const safePages = Number(pages) > 0 ? Number(pages) : 1;
  const safeLimit = Number(limit) > 0 ? Number(limit) : 10;
  const safeTotal = Number(total) >= 0 ? Number(total) : 0;
  const itemCount = Array.isArray(items) ? items.length : 0;
  const startIndex = safeTotal === 0 || itemCount === 0 ? 0 : (safePage - 1) * safeLimit + 1;
  const endIndex = safeTotal === 0 || itemCount === 0 ? 0 : Math.min(safeTotal, startIndex + itemCount - 1);
  let pageSizeOptionsResolved = Array.isArray(pageSizeOptions) && pageSizeOptions.length ? [...pageSizeOptions] : [5, 10, 15, 20];
  const pageSizeSet = new Set(pageSizeOptionsResolved);
  if (!pageSizeSet.has(safeLimit)) {
    pageSizeOptionsResolved.push(safeLimit);
  }
  pageSizeOptionsResolved = pageSizeOptionsResolved
    .map((value) => Number(value))
    .filter((value) => Number.isFinite(value) && value > 0)
    .sort((a, b) => a - b);
  const baseQueryParams = {};
  if (q) baseQueryParams.q = q;
  if (cat) baseQueryParams.cat = cat;
  if (state) baseQueryParams.state = state;
  if (Array.isArray(scannedRfids) && scannedRfids.length) {
    baseQueryParams.rfids = scannedRfids.join(',');
  }
  baseQueryParams.limit = safeLimit;
  baseQueryParams.page = safePage;
  const buildQuery = (overrides = {}) => {
    const params = { ...baseQueryParams };
    Object.entries(overrides || {}).forEach(([key, value]) => {
      if (value === null || value === undefined || value === '' || (typeof value === 'number' && !Number.isFinite(value))) {
        delete params[key];
      } else {
        params[key] = value;
      }
    });
    const entries = Object.entries(params)
      .filter(([, value]) => value !== undefined && value !== null && value !== '')
      .map(([key, value]) => `${encodeURIComponent(key)}=${encodeURIComponent(String(value))}`);
    return entries.length ? `?${entries.join('&')}` : '';
  };
%>

<div class="mb-2">
  <p class="text-sm opacity-70">Gestión de credocubes y componentes RFID</p>
  <form method="get" action="/inventario" class="mt-2" id="inv-search-form">
    <div class="form-control">
      <label class="input input-bordered flex items-center gap-2">
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="opacity-70"><circle cx="11" cy="11" r="8"></circle><path d="m21 21-4.3-4.3"></path></svg>
  <input type="search" class="grow" placeholder="Buscar / Escanear RFIDs (flujo continuo)" name="q" value="<%= q || '' %>" maxlength="24" id="inv-search-input" autocomplete="off" />
      </label>
    </div>
      <input type="hidden" name="state" value="<%= state %>" />
      <div class="flex flex-wrap gap-2" id="inv-rfid-tags">
        <% if (multiScan) { scannedRfids.forEach(r => { %>
          <span class="badge badge-primary gap-1" data-rfid-tag="<%= r %>">
            <%= r %>
            <button type="button" class="ml-1 text-error" data-action="remove-rfid" data-rfid="<%= r %>">✕</button>
          </span>
        <% }) } %>
      </div>
      <div class="text-xs opacity-70">
        <% if (multiScan) { %>
          Escaneando múltiples RFIDs ( <%= scannedRfids.length %> ). Cada nuevo bloque de 24 caracteres se añade automáticamente.
          <a href="/inventario" class="link link-error ml-2" id="inv-clear-multiscan">Limpiar</a>
        <% } else if (!cajaMode) { %>
          <a class="btn btn-sm <%= cat==='cubes' ? 'btn-primary' : '' %>" href="/inventario<%= buildQuery({ cat: 'cubes', page: 1 }) %>">Cubes (<%= counts?.cubes || 0 %>)</a>
          <a class="btn btn-sm <%= cat==='vips' ? 'btn-primary' : '' %>" href="/inventario<%= buildQuery({ cat: 'vips', page: 1 }) %>">VIPs (<%= counts?.vips || 0 %>)</a>
          <a class="btn btn-sm <%= cat==='tics' ? 'btn-primary' : '' %>" href="/inventario<%= buildQuery({ cat: 'tics', page: 1 }) %>">TICs (<%= counts?.tics || 0 %>)</a>
        <% } else { %>
          Categorías ocultas mientras está activo multi-scan o modo caja.
        <% } %>
      </div>
    <% if (!multiScan && !cajaMode) { %>
      <div class="mt-3 flex flex-wrap items-center gap-2">
        <label class="label text-xs opacity-70">Estado:</label>
        <div class="join">
          <a class="btn btn-xs join-item <%= !state ? 'btn-primary' : '' %>" href="/inventario<%= buildQuery({ state: '', page: 1 }) %>">Todos</a>
          <a class="btn btn-xs join-item <%= state==='inhabilitado' ? 'btn-primary' : '' %>" href="/inventario<%= buildQuery({ state: 'inhabilitado', page: 1 }) %>">Inhabilitado</a>
        </div>
      </div>
    <% } %>
  </form>
</div>

<div class="card bg-base-200 shadow-lg transition-all hover:shadow-xl">
  <div class="card-body animate-slide-up">
    <div class="overflow-x-auto">
      <table class="table table-zebra">
        <thead>
          <tr>
            <th>NOMBRE UNIDAD</th>
            <th>MODELO ID</th>
            <th>RFID</th>
            <th>LOTE</th>
            <th>ESTADO</th>
            <th>SUB-ESTADO</th>
            <th>ZONA</th>
            <th>SECCIÓN</th>
            <th>CATEGORÍA</th>
            <th>FECHA INGRESO</th>
            <th>ÚLTIMA ACTUALIZACIÓN</th>
            <th>ACTIVO</th>
            <th>ACCIONES</th>
          </tr>
        </thead>
        <tbody id="inv-tbody">
          <% if (items && items.length) { items.forEach(i => { %>
            <% 
              const fechaIngreso = i.fecha_ingreso ? new Date(i.fecha_ingreso).toLocaleString('es-CO', { timeZone: 'America/Bogota' }) : '-';
              const fechaIngresoIso = i.fecha_ingreso ? new Date(i.fecha_ingreso).toISOString() : '';
              const fechaActualizacion = i.ultima_actualizacion ? new Date(i.ultima_actualizacion).toLocaleString('es-CO', { timeZone: 'America/Bogota' }) : '-';
              const fechaActualizacionIso = i.ultima_actualizacion ? new Date(i.ultima_actualizacion).toISOString() : '';
              const activoTexto = i.activo === false ? 'Inhabilitado' : 'Habilitado';
              const activoTone = i.activo === false ? 'badge-error' : 'badge-success';
            %>
            <tr>
              <td><%= i.nombre_unidad %></td>
              <td><%= i.modelo_id %></td>
              <td><%= i.rfid %></td>
              <td class="whitespace-nowrap"><%= i.lote || '' %></td>
              <td class="whitespace-nowrap">
                <div class="badge badge-primary badge-outline hover:badge-secondary transition-colors whitespace-nowrap"><%= i.estado %></div>
              </td>
              <td class="whitespace-nowrap">
                <% if (i.sub_estado) { %>
                  <div class="badge badge-outline whitespace-nowrap"><%= i.sub_estado %></div>
                <% } else { %>
                  <span class="opacity-60">—</span>
                <% } %>
              </td>
              <td class="whitespace-nowrap">
                <% if (i.zona_nombre) { %><%= i.zona_nombre %><% } else { %><span class="opacity-60">—</span><% } %>
              </td>
              <td class="whitespace-nowrap">
                <% if (i.seccion_nombre) { %><%= i.seccion_nombre %><% } else { %><span class="opacity-60">—</span><% } %>
              </td>
              <td><%= i.categoria %></td>
              <td><%= fechaIngreso %></td>
              <td><%= fechaActualizacion %></td>
              <td><span class="badge badge-outline <%= activoTone %>"><%= activoTexto %></span></td>
              <td class="whitespace-nowrap">
                <div class="join">
                  <button type="button" class="btn btn-ghost btn-xs join-item" data-action="inv-edit"
                    data-id="<%= i.id %>"
                    data-modelo_id="<%= i.modelo_id %>"
                    data-rfid="<%- (i.rfid || '').replace(/\"/g,'&quot;') %>"
                    data-nombre="<%- (i.nombre_unidad || '').replace(/\"/g,'&quot;') %>"
                    data-lote="<%- (i.lote || '').replace(/\"/g,'&quot;') %>"
                    data-estado="<%- (i.estado || '').replace(/\"/g,'&quot;') %>"
                    data-sub="<%- (i.sub_estado || '').replace(/\"/g,'&quot;') %>"
                    data-zona-id="<%= i.zona_id || '' %>"
                    data-seccion-id="<%= i.seccion_id || '' %>"
                    data-fecha-ingreso="<%= fechaIngresoIso %>"
                    data-ultima-actualizacion="<%= fechaActualizacionIso %>"
                    data-activo="<%= i.activo === false ? '0' : '1' %>">
                    ✎
                  </button>
                  <form method="post" action="/inventario/<%= i.id %>/delete" class="join-item inv-delete-form">
                    <button class="btn btn-ghost btn-xs text-error" aria-label="Inhabilitar" title="Inhabilitar">🗑</button>
                  </form>
                </div>
              </td>
            </tr>
          <% }) } else { %>
            <tr><td colspan="13" class="text-center py-4 opacity-60">Sin resultados</td></tr>
          <% } %>
        </tbody>
      </table>
    </div>
    <div class="mt-4 space-y-3 text-xs">
      <div class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
        <div id="inv-status">
          <% if (itemCount === 0) { %>
            Mostrando 0 de <%= safeTotal %> resultados
          <% } else { %>
            Mostrando <%= startIndex %>-<%= endIndex %> de <%= safeTotal %> resultados
          <% } %>
        </div>
        <div class="text-right"><span class="badge badge-outline" id="inv-mode-badge">Inicial</span></div>
      </div>
      <div class="flex flex-col items-start gap-3 md:flex-row md:items-center md:justify-between">
        <form id="inv-limit-form" method="get" action="/inventario" class="flex items-center gap-2">
          <input type="hidden" name="page" value="1" />
          <% if (q) { %><input type="hidden" name="q" value="<%= q %>" /><% } %>
          <% if (cat) { %><input type="hidden" name="cat" value="<%= cat %>" /><% } %>
          <% if (state) { %><input type="hidden" name="state" value="<%= state %>" /><% } %>
          <% if (Array.isArray(scannedRfids) && scannedRfids.length) { %>
            <input type="hidden" name="rfids" value="<%= scannedRfids.join(',') %>" />
          <% } %>
          <label for="inv-limit-select" class="label text-xs opacity-70 p-0">Resultados por página:</label>
          <select id="inv-limit-select" name="limit" class="select select-xs select-bordered w-auto">
            <% pageSizeOptionsResolved.forEach((opt) => { %>
              <option value="<%= opt %>" <%= opt === safeLimit ? 'selected' : '' %>><%= opt %></option>
            <% }) %>
          </select>
        </form>
        <div class="join">
          <% if (safePage > 1) { %>
            <a class="btn btn-xs join-item" href="/inventario<%= buildQuery({ page: safePage - 1 }) %>">Anterior</a>
          <% } else { %>
            <span class="btn btn-xs join-item btn-disabled opacity-60">Anterior</span>
          <% } %>
          <span class="btn btn-xs join-item btn-disabled">Página <%= safePage %> de <%= safePages %></span>
          <% if (safePage < safePages) { %>
            <a class="btn btn-xs join-item" href="/inventario<%= buildQuery({ page: safePage + 1 }) %>">Siguiente</a>
          <% } else { %>
            <span class="btn btn-xs join-item btn-disabled opacity-60">Siguiente</span>
          <% } %>
        </div>
      </div>
    </div>
  </div>
</div>

<!-- Modal: Editar item inventario -->
<dialog id="inv-edit-modal" class="modal">
  <div class="modal-box max-w-lg">
    <form method="dialog">
      <button class="btn btn-sm btn-circle btn-ghost absolute right-2 top-2">✕</button>
    </form>
    <h3 class="font-bold text-lg mb-2">Editar item</h3>
    <form id="inv-edit-form" method="post" action="/inventario/0/update" class="space-y-3">
      <div class="grid grid-cols-2 gap-3">
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Modelo ID</span>
          <input id="inv-edit-modelo_id" name="modelo_id" type="text" class="input input-sm input-bordered" readonly />
        </label>
        <label class="form-control">
          <span class="label-text text-xs opacity-70">RFID</span>
          <input id="inv-edit-rfid" name="rfid" type="text" class="input input-sm input-bordered" readonly />
        </label>
      </div>
      <div class="grid grid-cols-2 gap-3">
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Nombre unidad</span>
          <input id="inv-edit-nombre" name="nombre_unidad" type="text" class="input input-sm input-bordered" autocomplete="off" />
        </label>
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Lote</span>
          <input id="inv-edit-lote" name="lote" type="text" class="input input-sm input-bordered" autocomplete="off" />
        </label>
      </div>
      <div class="grid grid-cols-2 gap-3">
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Estado</span>
          <input id="inv-edit-estado" name="estado" type="text" class="input input-sm input-bordered input-disabled" autocomplete="off" readonly />
        </label>
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Sub-estado</span>
          <input id="inv-edit-sub" name="sub_estado" type="text" class="input input-sm input-bordered input-disabled" autocomplete="off" readonly />
        </label>
      </div>
      <div class="grid grid-cols-2 gap-3">
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Zona</span>
          <select id="inv-edit-zona" name="zona_id" class="select select-sm select-bordered">
            <option value="">Sin zona</option>
          </select>
        </label>
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Sección</span>
          <select id="inv-edit-seccion" name="seccion_id" class="select select-sm select-bordered">
            <option value="">Sin sección</option>
          </select>
        </label>
      </div>
      <div class="grid grid-cols-2 gap-3">
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Fecha ingreso</span>
          <input id="inv-edit-fecha-ingreso" type="text" class="input input-sm input-bordered input-disabled" readonly />
        </label>
        <label class="form-control">
          <span class="label-text text-xs opacity-70">Última actualización</span>
          <input id="inv-edit-ultima" type="text" class="input input-sm input-bordered input-disabled" readonly />
        </label>
      </div>
      <div class="form-control">
        <span class="label-text text-xs opacity-70">Disponibilidad</span>
        <div class="flex items-center gap-3">
          <input type="checkbox" class="toggle toggle-sm" id="inv-edit-activo-toggle" aria-label="Alternar disponibilidad" />
          <span id="inv-edit-activo-label" class="text-xs font-semibold">Habilitado</span>
          <input type="hidden" name="activo" id="inv-edit-activo" value="1" />
        </div>
      </div>
      <p id="inv-edit-location-hint" class="text-xs opacity-60"></p>
      <div class="flex items-center justify-between mt-2">
        <span id="inv-edit-msg" class="text-xs opacity-70"></span>
        <div class="join">
          <button type="button" class="btn btn-ghost btn-sm join-item" id="inv-edit-cancel">Cancelar</button>
          <button type="submit" class="btn btn-primary btn-sm join-item">Guardar</button>
        </div>
      </div>
    </form>
  </div>
  <form method="dialog" class="modal-backdrop"><button>close</button></form>
</dialog>

<script src="/static/js/inventario.js?v=<%= assetVersion %>"></script>
